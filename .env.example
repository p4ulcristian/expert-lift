# Expert Lift Environment Configuration
# Copy this file to .env and configure all required values for your environment

# ═══════════════════════════════════════════════════════════════════════════
# Core Application Settings
# ═══════════════════════════════════════════════════════════════════════════

# Development mode flag (true/false)
# Set to false for production
EXPERTLIFT_DEV=false

# Server port (default: 4000 for production)
EXPERTLIFT_PORT=4000

# Application domain (without https://)
# Used for CORS, Auth0 callbacks, and absolute URLs
EXPERTLIFT_DOMAIN=yourdomain.com

# ═══════════════════════════════════════════════════════════════════════════
# Database Configuration (PostgreSQL)
# ═══════════════════════════════════════════════════════════════════════════

# Database host (e.g., localhost, db container name, or RDS endpoint)
EXPERTLIFT_DB_HOST=localhost

# Database port (default PostgreSQL port is 5432)
EXPERTLIFT_DB_PORT=5432

# Database name
EXPERTLIFT_DB_NAME=expert_lift

# Database user with read/write permissions
EXPERTLIFT_DB_USER=expert_lift_user

# Database password (use a strong password in production)
EXPERTLIFT_DB_PASSWORD=your_secure_database_password

# ═══════════════════════════════════════════════════════════════════════════
# Auth0 Authentication (Multiple Applications)
# ═══════════════════════════════════════════════════════════════════════════

# Auth0 Customizer Application
# Create separate Auth0 applications for each module
EXPERTLIFT_AUTH0_CUSTOMIZER_CLIENT_ID=your_customizer_client_id
EXPERTLIFT_AUTH0_CUSTOMIZER_SECRET=your_customizer_client_secret

# Auth0 Flex Application (Business Management)
EXPERTLIFT_AUTH0_FLEX_CLIENT_ID=your_flex_client_id
EXPERTLIFT_AUTH0_FLEX_SECRET=your_flex_client_secret

# Auth0 Labs Application
EXPERTLIFT_AUTH0_LABS_CLIENT_ID=your_labs_client_id
EXPERTLIFT_AUTH0_LABS_SECRET=your_labs_client_secret

# ═══════════════════════════════════════════════════════════════════════════
# File Storage (MinIO S3-Compatible)
# ═══════════════════════════════════════════════════════════════════════════

# MinIO server URL (e.g., http://localhost:9000 or https://minio.yourdomain.com)
EXPERTLIFT_MINIO_URL=https://minio.yourdomain.com

# MinIO API endpoint (typically same as URL without protocol)
EXPERTLIFT_MINIO_ENDPOINT=minio.yourdomain.com

# MinIO access key (equivalent to AWS access key)
EXPERTLIFT_MINIO_ACCESS_KEY=your_minio_access_key

# MinIO secret key (equivalent to AWS secret key)
EXPERTLIFT_MINIO_SECRET_KEY=your_minio_secret_key

# ═══════════════════════════════════════════════════════════════════════════
# Email Service (SMTP Configuration)
# ═══════════════════════════════════════════════════════════════════════════

# SMTP server hostname (e.g., smtp.gmail.com, smtp.sendgrid.net)
EXPERTLIFT_EMAIL_HOST=smtp.yourdomain.com

# SMTP username (often same as email address)
EXPERTLIFT_EMAIL_USERNAME=noreply@yourdomain.com

# SMTP password or app-specific password
EXPERTLIFT_EMAIL_PASSWORD=your_smtp_password

# ═══════════════════════════════════════════════════════════════════════════
# Production Deployment Notes
# ═══════════════════════════════════════════════════════════════════════════

# 1. Database Setup:
#    - Create PostgreSQL database and user
#    - Ensure user has CREATE, INSERT, UPDATE, DELETE permissions
#    - Run database migrations after first deployment

# 2. Auth0 Configuration:
#    - Set up separate Auth0 applications for each module (customizer, flex, labs)
#    - Configure callback URLs: https://EXPERTLIFT_DOMAIN/auth/callback
#    - Enable appropriate OAuth providers (Google, GitHub, etc.)

# 3. MinIO Setup:
#    - Deploy MinIO server or use AWS S3
#    - Create buckets for file storage
#    - Configure bucket policies for public/private access

# 4. Email Configuration:
#    - Configure SMTP server (Gmail, SendGrid, AWS SES, etc.)
#    - For Gmail: Use app-specific passwords, not account password
#    - Test email delivery in staging environment

# 5. Security Checklist:
#    - Use strong passwords for all services
#    - Enable SSL/TLS for all external connections
#    - Regularly rotate secrets and API keys
#    - Monitor access logs for suspicious activity

# 7. Monitoring:
#    - Set up log aggregation (ELK stack, Splunk, etc.)
#    - Configure application performance monitoring
#    - Set up alerts for critical errors and downtime